__pycache__/
*.pyc
.venv/
.uv/
.DS_Store
/tmp/
chess-analysis/
games*.json
analysis*.json
aggregate*.json
*.pgn
# per-user report/coaching outputs carry the username in the filename — keep them
# out of git even if rendered outside chess-analysis/. Does not match the
# committed docs/sample-report.html (it doesn't start with "report-").
report-*.html
tips-*.md
!tests/fixtures/*.json
!tests/fixtures/*.pgn

# planning artifacts — kept local, out of PRs
docs/superpowers/
